<Window x:Class="BlueBirdsERP.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:BlueBirdsERP.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="{Binding ProductName}"
        Height="820"
        MinHeight="700"
        Width="1360"
        MinWidth="1120"
        Background="{StaticResource AppBackgroundBrush}">
    <Window.Resources>
        <Style x:Key="IconTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource IconFontFamily}" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="SectionBorderStyle" TargetType="Border" BasedOn="{StaticResource CardBorderStyle}">
            <Setter Property="Padding" Value="18" />
            <Setter Property="Margin" Value="0,0,0,16" />
        </Style>

        <Style x:Key="PillBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceLowBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource SoftBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="10,6" />
        </Style>

        <Style x:Key="PageHeadingStyle" TargetType="TextBlock" BasedOn="{StaticResource HeadlineMediumStyle}" />

        <DataTemplate DataType="{x:Type vm:DashboardPageViewModel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="{Binding Title}" Style="{StaticResource PageHeadingStyle}" Margin="0,0,0,16" />

                    <ItemsControl ItemsSource="{Binding Metrics}" Margin="0,0,0,16">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="4" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource CardBorderStyle}" Padding="16" Margin="0,0,12,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="38" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Label}" Style="{StaticResource LabelTextStyle}" />
                                            <TextBlock Text="{Binding Value}" FontSize="24" FontWeight="SemiBold" Margin="0,10,0,4" />
                                            <TextBlock Text="{Binding Detail}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
                                        </StackPanel>
                                        <Border Grid.Column="1" Width="38" Height="38" Background="{StaticResource SecondaryContainerBrush}" CornerRadius="4">
                                            <TextBlock Text="{Binding Icon}" FontFamily="{StaticResource IconFontFamily}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.4*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="0.8*" />
                        </Grid.ColumnDefinitions>

                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="Recent Invoices" Style="{StaticResource HeadlineSmallStyle}" Margin="0,0,0,12" />
                                <ItemsControl ItemsSource="{Binding RecentInvoices}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid MinHeight="42">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="1.8*" />
                                                    <ColumnDefinition Width="0.8*" />
                                                    <ColumnDefinition Width="0.8*" />
                                                    <ColumnDefinition Width="0.7*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding InvoiceNumber}" FontWeight="SemiBold" VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="1" Text="{Binding Channel}" VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="2" Text="{Binding Amount}" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                                <TextBlock Grid.Column="3" Text="{Binding Status}" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="Online Queue" Style="{StaticResource HeadlineSmallStyle}" Margin="0,0,0,12" />
                                <ItemsControl ItemsSource="{Binding QueueRows}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding Status}" />
                                                <TextBlock Grid.Column="1" Text="{Binding Count}" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:PosPageViewModel}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.35*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="0.85*" />
                </Grid.ColumnDefinitions>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" Style="{StaticResource PageHeadingStyle}" Margin="0,0,0,16" />
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <Grid Margin="0,0,0,14">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="160" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="160" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Message}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" VerticalAlignment="Center" />
                                    <ComboBox Grid.Column="1" ItemsSource="{Binding SaleChannels}" SelectedItem="{Binding SelectedSaleChannel}" />
                                    <ComboBox Grid.Column="3" ItemsSource="{Binding PaymentMethods}" SelectedItem="{Binding SelectedPaymentMethod}" />
                                </Grid>

                                <ListBox ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" Height="410" BorderBrush="{StaticResource SoftBorderBrush}" BorderThickness="1">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="10" BorderBrush="{StaticResource SoftBorderBrush}" BorderThickness="0,0,0,1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="120" />
                                                        <ColumnDefinition Width="120" />
                                                        <ColumnDefinition Width="120" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold" />
                                                        <TextBlock Text="{Binding BatchReference}" Style="{StaticResource LabelTextStyle}" Margin="0,3,0,0" />
                                                    </StackPanel>
                                                    <TextBlock Grid.Column="1" Text="{Binding PriceText}" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                                    <TextBlock Grid.Column="2" Text="{Binding StockText}" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                                    <TextBlock Grid.Column="3" Text="{Binding Unit}" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>

                                <Grid Margin="0,14,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox Text="{Binding Quantity}" />
                                    <Button Grid.Column="2" Content="Add Item" Command="{Binding AddSelectedProductCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                                    <ComboBox Grid.Column="4" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" DisplayMemberPath="Name" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Column="2" Style="{StaticResource SectionBorderStyle}">
                    <DockPanel LastChildFill="True">
                        <StackPanel DockPanel.Dock="Top">
                            <TextBlock Text="Current Invoice" Style="{StaticResource HeadlineSmallStyle}" Margin="0,0,0,12" />
                            <ItemsControl ItemsSource="{Binding CartLines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="90" />
                                                <ColumnDefinition Width="34" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold" />
                                                <TextBlock Text="{Binding QuantityText}" Foreground="{StaticResource MutedTextBrush}" />
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding LineTotalText}" FontWeight="SemiBold" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                            <Button Grid.Column="2" Content="&#xE74D;" FontFamily="{StaticResource IconFontFamily}" Command="{Binding DataContext.RemoveLineCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Padding="6" MinHeight="28" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <StackPanel DockPanel.Dock="Bottom">
                            <Border BorderBrush="{StaticResource SoftBorderBrush}" BorderThickness="0,1,0,0" Margin="0,8,0,12" />
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Total" FontSize="18" FontWeight="SemiBold" />
                                <TextBlock Grid.Column="1" Text="{Binding CartTotalText}" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" />
                            </Grid>
                            <TextBlock Text="Cash" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,4" />
                            <TextBox Text="{Binding CashAmount}" Margin="0,0,0,8" />
                            <TextBlock Text="Card" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,4" />
                            <TextBox Text="{Binding CardAmount}" Margin="0,0,0,8" />
                            <TextBlock Text="Credit" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,4" />
                            <TextBox Text="{Binding CreditAmount}" Margin="0,0,0,8" />
                            <TextBlock Text="Due Date" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,4" />
                            <TextBox Text="{Binding ManualDueDate}" Margin="0,0,0,12" />
                            <Button Content="Checkout" Command="{Binding CheckoutCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,8" />
                            <Button Content="Preview Receipt PDF" Command="{Binding PreviewReceiptCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,0,8" />
                            <Button Content="Clear Invoice" Command="{Binding ClearCartCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                        </StackPanel>
                    </DockPanel>
                </Border>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:InventoryPageViewModel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="{Binding Title}" Style="{StaticResource PageHeadingStyle}" Margin="0,0,0,16" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.1*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="0.9*" />
                        </Grid.ColumnDefinitions>
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="Stock Levels" Style="{StaticResource HeadlineSmallStyle}" Margin="0,0,0,12" />
                                <ListBox ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" Height="260" BorderBrush="{StaticResource SoftBorderBrush}" BorderThickness="1">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="110" />
                                                    <ColumnDefinition Width="110" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold" />
                                                <TextBlock Grid.Column="1" Text="{Binding RemainingText}" HorizontalAlignment="Right" />
                                                <TextBlock Grid.Column="2" Text="{Binding ReorderText}" HorizontalAlignment="Right" Foreground="{StaticResource MutedTextBrush}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="Manual Purchase" Style="{StaticResource HeadlineSmallStyle}" Margin="0,0,0,12" />
                                <TextBlock Text="{Binding Message}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,0,0,12" />
                                <TextBlock Text="Quantity" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,4" />
                                <TextBox Text="{Binding NewBatchQuantity}" Margin="0,0,0,8" />
                                <TextBlock Text="Cost Price" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,4" />
                                <TextBox Text="{Binding NewBatchCost}" Margin="0,0,0,8" />
                                <TextBlock Text="Expiry Date" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,4" />
                                <TextBox Text="{Binding NewBatchExpiry}" Margin="0,0,0,12" />
                                <Button Content="Record Batch" Command="{Binding RecordBatchCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                            </StackPanel>
                        </Border>
                    </Grid>
                    <Border Style="{StaticResource SectionBorderStyle}">
                        <StackPanel>
                            <TextBlock Text="Batch Register" Style="{StaticResource HeadlineSmallStyle}" Margin="0,0,0,12" />
                            <ItemsControl ItemsSource="{Binding Batches}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid MinHeight="38">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="130" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="130" />
                                                <ColumnDefinition Width="110" />
                                                <ColumnDefinition Width="110" />
                                                <ColumnDefinition Width="90" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Batch}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="1" Text="{Binding ProductName}" FontWeight="SemiBold" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="2" Text="{Binding Remaining}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="3" Text="{Binding Cost}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="4" Text="{Binding ExpiryDate}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="5" Text="{Binding Status}" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:CreditorsPageViewModel}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.3*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="0.8*" />
                </Grid.ColumnDefinitions>
                <Border Style="{StaticResource SectionBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" Style="{StaticResource PageHeadingStyle}" Margin="0,0,0,16" />
                        <TextBlock Text="{Binding Message}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,0,0,12" />
                        <ListBox ItemsSource="{Binding OutstandingInvoices}" SelectedItem="{Binding SelectedInvoice}" BorderBrush="{StaticResource SoftBorderBrush}" BorderThickness="1">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="160" />
                                            <ColumnDefinition Width="120" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding CustomerName}" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding InvoiceNumber}" Foreground="{StaticResource MutedTextBrush}" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding DueDate}" VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="2" Text="{Binding BalanceText}" FontWeight="SemiBold" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Border>
                <StackPanel Grid.Column="2">
                    <Border Style="{StaticResource SectionBorderStyle}">
                        <StackPanel>
                            <TextBlock Text="Record Payment" Style="{StaticResource HeadlineSmallStyle}" Margin="0,0,0,12" />
                            <TextBlock Text="Amount" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,4" />
                            <TextBox Text="{Binding PaymentAmount}" Margin="0,0,0,8" />
                            <TextBlock Text="Method" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,4" />
                            <ComboBox ItemsSource="{Binding PaymentMethods}" SelectedItem="{Binding PaymentMethod}" Margin="0,0,0,12" />
                            <Button Content="Apply Payment" Command="{Binding RecordPaymentCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource SectionBorderStyle}">
                        <StackPanel>
                            <TextBlock Text="Debtor Aging" Style="{StaticResource HeadlineSmallStyle}" Margin="0,0,0,12" />
                            <ItemsControl ItemsSource="{Binding DebtorBuckets}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="90" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Bucket}" />
                                            <TextBlock Grid.Column="1" Text="{Binding Amount}" HorizontalAlignment="Right" FontWeight="SemiBold" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:ReportsPageViewModel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="{Binding Title}" Style="{StaticResource PageHeadingStyle}" Margin="0,0,0,16" />
                    <Border Style="{StaticResource SectionBorderStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="170" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="170" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <DatePicker SelectedDate="{Binding FromDate}" />
                            <DatePicker Grid.Column="2" SelectedDate="{Binding ToDate}" />
                            <Button Grid.Column="4" Content="Run Report" Command="{Binding RunReportCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                            <TextBlock Grid.Column="5" Text="{Binding Message}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" VerticalAlignment="Center" Margin="12,0,0,0" />
                        </Grid>
                    </Border>
                    <ItemsControl ItemsSource="{Binding Metrics}" Margin="0,0,0,16">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="4" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource CardBorderStyle}" Padding="16" Margin="0,0,12,0">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Label}" Style="{StaticResource LabelTextStyle}" />
                                        <TextBlock Text="{Binding Value}" FontSize="23" FontWeight="SemiBold" Margin="0,10,0,4" />
                                        <TextBlock Text="{Binding Detail}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="Stock On Hand" Style="{StaticResource HeadlineSmallStyle}" Margin="0,0,0,12" />
                                <ItemsControl ItemsSource="{Binding StockRows}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="120" />
                                                    <ColumnDefinition Width="120" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold" />
                                                <TextBlock Grid.Column="1" Text="{Binding Remaining}" HorizontalAlignment="Right" />
                                                <TextBlock Grid.Column="2" Text="{Binding ReorderLevel}" HorizontalAlignment="Right" Foreground="{StaticResource MutedTextBrush}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="Audit Activity" Style="{StaticResource HeadlineSmallStyle}" Margin="0,0,0,12" />
                                <ItemsControl ItemsSource="{Binding AuditRows}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="60" />
                                                    <ColumnDefinition Width="120" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding Time}" Style="{StaticResource LabelTextStyle}" />
                                                <TextBlock Grid.Column="1" Text="{Binding Module}" />
                                                <TextBlock Grid.Column="2" Text="{Binding Action}" FontWeight="SemiBold" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:SettingsPageViewModel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="{Binding Title}" Style="{StaticResource PageHeadingStyle}" Margin="0,0,0,16" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.1*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="0.9*" />
                        </Grid.ColumnDefinitions>
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="System Setting" Style="{StaticResource HeadlineSmallStyle}" Margin="0,0,0,12" />
                                <TextBlock Text="{Binding Message}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,0,0,12" />
                                <TextBlock Text="Key" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,4" />
                                <TextBox Text="{Binding SettingKey}" Margin="0,0,0,8" />
                                <TextBlock Text="Value" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,4" />
                                <TextBox Text="{Binding SettingValue}" Margin="0,0,0,12" />
                                <Button Content="Save Setting" Command="{Binding SaveSettingCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="Database" Style="{StaticResource HeadlineSmallStyle}" Margin="0,0,0,12" />
                                <Button Content="Test SQLite" Command="{Binding TestSqliteCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,0,8" />
                                <Button Content="Backup SQLite" Command="{Binding BackupDatabaseCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,0,12" />
                                <ItemsControl ItemsSource="{Binding DatabaseRows}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="90" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                                    <TextBlock Text="{Binding Detail}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
                                                </StackPanel>
                                                <TextBlock Grid.Column="1" Text="{Binding Status}" HorizontalAlignment="Right" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </Grid>
                    <Border Style="{StaticResource SectionBorderStyle}">
                        <StackPanel>
                            <TextBlock Text="Saved Settings" Style="{StaticResource HeadlineSmallStyle}" Margin="0,0,0,12" />
                            <ItemsControl ItemsSource="{Binding Settings}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid MinHeight="38">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="1.2*" />
                                                <ColumnDefinition Width="120" />
                                                <ColumnDefinition Width="150" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Key}" FontWeight="SemiBold" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="1" Text="{Binding Value}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="2" Text="{Binding Type}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="3" Text="{Binding UpdatedAt}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid Visibility="{Binding IsLoginVisible, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Background="{StaticResource SurfaceLowestBrush}" BorderBrush="{StaticResource SoftBorderBrush}" BorderThickness="0,0,1,0" Padding="32">
                <DockPanel LastChildFill="True">
                    <StackPanel DockPanel.Dock="Top">
                        <Border Width="52" Height="52" Background="{StaticResource PrimaryBrush}" CornerRadius="4" HorizontalAlignment="Left">
                            <TextBlock Text="&#xE719;" FontFamily="{StaticResource IconFontFamily}" FontSize="22" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <TextBlock Text="{Binding ProductName}" Style="{StaticResource HeadlineMediumStyle}" Margin="0,18,0,4" />
                        <TextBlock Text="{Binding CompanyName}" Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Bottom">
                        <Border Style="{StaticResource PillBorderStyle}" Margin="0,0,0,8">
                            <TextBlock Text="SQLite primary, offline first" />
                        </Border>
                        <Border Style="{StaticResource PillBorderStyle}">
                            <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" />
                        </Border>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Grid Grid.Column="1" Margin="72">
                <Border Width="420" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource CardBorderStyle}" Padding="24">
                    <StackPanel>
                        <TextBlock Text="Sign In" Style="{StaticResource HeadlineMediumStyle}" Margin="0,0,0,6" />
                        <TextBlock Text="Use an active Admin or Cashier account." Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,24" />
                        <TextBlock Text="Username" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,6" />
                        <TextBox Text="{Binding LoginUsername, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />
                        <TextBlock Text="Password" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,6" />
                        <PasswordBox PasswordChanged="LoginPasswordBox_OnPasswordChanged" Margin="0,0,0,14" />
                        <TextBlock Text="{Binding LoginError}" Foreground="{StaticResource ErrorBrush}" TextWrapping="Wrap" Margin="0,0,0,14" />
                        <Button Content="Sign In" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <Grid Visibility="{Binding IsAuthenticated, Converter={StaticResource BoolToVisibilityConverter}}" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="248" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Background="{StaticResource SurfaceLowestBrush}" BorderBrush="{StaticResource SoftBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                <DockPanel LastChildFill="True">
                    <StackPanel DockPanel.Dock="Top">
                        <Grid Margin="0,0,0,18">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Width="40" Height="40" Background="{StaticResource PrimaryBrush}" CornerRadius="4">
                                <TextBlock Text="&#xE719;" FontFamily="{StaticResource IconFontFamily}" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                <TextBlock Text="{Binding ProductName}" FontSize="17" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding CompanyName}" Foreground="{StaticResource MutedTextBrush}" />
                            </StackPanel>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding NavigationItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Visibility="{Binding IsVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                                            Margin="0,0,0,8"
                                            HorizontalContentAlignment="Stretch">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                                <Setter Property="Background" Value="{StaticResource SurfaceLowestBrush}" />
                                                <Setter Property="BorderBrush" Value="{StaticResource SoftBorderBrush}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource SecondaryContainerBrush}" />
                                                        <Setter Property="BorderBrush" Value="{StaticResource SecondaryContainerBrush}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="26" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Icon}" Style="{StaticResource IconTextStyle}" />
                                            <TextBlock Grid.Column="1" Text="{Binding Title}" FontWeight="SemiBold" />
                                        </Grid>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Bottom">
                        <Border Style="{StaticResource PillBorderStyle}" Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock Text="{Binding CurrentUserDisplay}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding CurrentRoleDisplay}" Style="{StaticResource LabelTextStyle}" />
                            </StackPanel>
                        </Border>
                        <Button Content="Logout" Command="{Binding LogoutCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="16" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Background="{StaticResource SurfaceLowestBrush}" BorderBrush="{StaticResource SoftBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{Binding CurrentPage.Title}" FontSize="18" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource MutedTextBrush}" />
                        </StackPanel>
                        <Border Grid.Column="1" Style="{StaticResource PillBorderStyle}">
                            <TextBlock Text="Offline Ready" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" />
                        </Border>
                    </Grid>
                </Border>

                <ContentControl Grid.Row="2" Content="{Binding CurrentPage}" />
            </Grid>
        </Grid>
    </Grid>
</Window>
