<UserControl x:Class="BlueBirdsERP.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BlueBirdsERP.Desktop.ViewModels">

    <Grid Background="{StaticResource SurfaceContainerLowBrush}">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Style="{StaticResource CardStyle}" Width="400" Padding="40,36">
                <StackPanel>
                    <!-- App title -->
                    <TextBlock Text="PoultryPro ERP"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,4" />

                    <TextBlock Text="Sign in to your account"
                               FontSize="14"
                               Foreground="{StaticResource OnSurfaceVariantBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,32" />

                    <!-- Username -->
                    <TextBlock Text="USERNAME" Style="{StaticResource LabelStyle}" />
                    <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource InputTextBoxStyle}"
                             Margin="0,0,0,20" />

                    <!-- Password -->
                    <TextBlock Text="PASSWORD" Style="{StaticResource LabelStyle}" />
                    <PasswordBox x:Name="PasswordBox"
                                 Padding="12,10"
                                 FontSize="14"
                                 Foreground="{StaticResource OnSurfaceBrush}"
                                 Background="{StaticResource SurfaceContainerLowestBrush}"
                                 BorderBrush="{StaticResource OutlineVariantBrush}"
                                 BorderThickness="1"
                                 Margin="0,0,0,8" />

                    <!-- Error message -->
                    <Border Background="{StaticResource ErrorContainerBrush}"
                            CornerRadius="4"
                            Padding="12,8"
                            Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}"
                            Margin="0,0,0,16">
                        <TextBlock Text="{Binding ErrorMessage}"
                                   Foreground="{StaticResource OnErrorContainerBrush}"
                                   FontSize="13"
                                   TextWrapping="Wrap" />
                    </Border>

                    <!-- Bootstrap message -->
                    <Border Background="{StaticResource SuccessContainerBrush}"
                            CornerRadius="4"
                            Padding="12,8"
                            Visibility="{Binding HasBootstrapMessage, Converter={StaticResource BoolToVis}}"
                            Margin="0,0,0,16">
                        <TextBlock Text="{Binding BootstrapMessage}"
                                   Foreground="{StaticResource SuccessBrush}"
                                   FontSize="13"
                                   TextWrapping="Wrap" />
                    </Border>

                    <!-- Sign In button -->
                    <Button x:Name="SignInButton"
                            Content="Sign In"
                            Style="{StaticResource PrimaryButtonStyle}"
                            HorizontalAlignment="Stretch"
                            Margin="0,0,0,16"
                            Click="SignInButton_Click" />

                    <!-- Bootstrap link -->
                    <Button Content="First time? Initialize admin account"
                            HorizontalAlignment="Center"
                            Command="{Binding BootstrapCommand}"
                            Style="{StaticResource GhostButtonStyle}"
                            FontSize="12"
                            Padding="4,2" />

                    <!-- Loading indicator -->
                    <TextBlock Text="Signing in..."
                               Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"
                               HorizontalAlignment="Center"
                               Foreground="{StaticResource PrimaryBrush}"
                               FontSize="13"
                               Margin="0,12,0,0" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
