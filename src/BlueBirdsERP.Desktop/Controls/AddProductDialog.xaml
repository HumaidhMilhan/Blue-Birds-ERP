<Window x:Class="BlueBirdsERP.Desktop.Controls.AddProductDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Product" Width="450" Height="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource SurfaceBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Add New Product" Style="{StaticResource SectionHeaderStyle}" />

        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="Product Name" Style="{StaticResource LabelStyle}" />
            <TextBox x:Name="NameBox" Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,12" />

            <TextBlock Text="Pricing Type" Style="{StaticResource LabelStyle}" />
            <ComboBox x:Name="PricingTypeBox" Margin="0,0,0,12">
                <ComboBoxItem Content="WeightBased" IsSelected="True" />
                <ComboBoxItem Content="UnitBased" />
            </ComboBox>

            <TextBlock Text="Unit of Measure (e.g. kg, piece, tray)" Style="{StaticResource LabelStyle}" />
            <TextBox x:Name="UoMBox" Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,12" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Selling Price" Style="{StaticResource LabelStyle}" />
                    <TextBox x:Name="PriceBox" Style="{StaticResource InputTextBoxStyle}" />
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Reorder Level" Style="{StaticResource LabelStyle}" />
                    <TextBox x:Name="ReorderBox" Style="{StaticResource InputTextBoxStyle}" />
                </StackPanel>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Margin="0,0,12,0"
                    Click="Cancel_Click" />
            <Button Content="Create"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="Create_Click" />
        </StackPanel>
    </Grid>
</Window>
