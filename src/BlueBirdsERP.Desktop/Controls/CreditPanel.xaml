<UserControl x:Class="BlueBirdsERP.Desktop.Controls.CreditPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Style="{StaticResource CardStyle}"
            Visibility="{Binding CreditSummary, Converter={StaticResource BoolToVis}}"
            Margin="0,8,0,0">
        <StackPanel>
            <TextBlock Text="Credit Account"
                       FontWeight="SemiBold"
                       FontSize="14"
                       Foreground="{StaticResource PrimaryBrush}"
                       Margin="0,0,0,12" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,8">
                    <TextBlock Text="Outstanding Balance" Style="{StaticResource LabelStyle}" />
                    <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}">
                        <Run Text="{Binding CreditSummary.OutstandingBalance, StringFormat=Rs. {0:N2}}" />
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,8">
                    <TextBlock Text="Credit Limit" Style="{StaticResource LabelStyle}" />
                    <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}">
                        <Run Text="{Binding CreditSummary.CreditLimit, StringFormat=Rs. {0:N2}}" />
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,8">
                    <TextBlock Text="Available Credit" Style="{StaticResource LabelStyle}" />
                    <TextBlock FontSize="16" FontWeight="SemiBold">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource SuccessBrush}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CreditSummary.IsLimitAlertActive}" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource ErrorBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                        <Run Text="{Binding CreditSummary.AvailableCredit, StringFormat=Rs. {0:N2}}" />
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,8">
                    <TextBlock Text="Overdue Invoices" Style="{StaticResource LabelStyle}" />
                    <TextBlock FontSize="16" FontWeight="SemiBold">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CreditSummary.OverdueInvoiceCount}" Value="0">
                                        <Setter Property="Foreground" Value="{StaticResource SuccessBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                        <Run Text="{Binding CreditSummary.OverdueInvoiceCount}" />
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
                    <TextBlock Text="Last Payment" Style="{StaticResource LabelStyle}" />
                    <TextBlock FontSize="14">
                        <Run Text="{Binding CreditSummary.LastPaymentDate, StringFormat='{}{0:yyyy-MM-dd}', FallbackValue='No payments'}" />
                    </TextBlock>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
