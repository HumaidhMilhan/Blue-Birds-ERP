# Database

This folder is reserved for migration notes, schema exports, seed data, and ERD artifacts.

The default central database is PostgreSQL. The offline POS store is SQLite and should be treated as a local queue/cache for POS operation during connectivity loss.

